{
  "name": "home-turor-app",
  "version": "0.1.0",
  "private": false,
  "homepage": "https://shantoatiq.github.io/Home-Turor-App",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
